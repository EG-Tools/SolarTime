# Solar Time v0.23

- 시계 표기: 24시간 / 12시간 선택을 화면 설정에 추가했습니다. 12시간 모드에서는 AM/PM을 함께 표시합니다.
- 1일/초 같은 고배속에서 표면 렌더 작업을 매 화면 프레임마다 무제한 제출하지 않고 일정한 생산 주기로 제한했습니다.
- 추적하지 않는 천체의 표면 래스터 상한을 256px로 낮추고 화면상 큰 천체를 먼저 처리해 Worker/GPU 부하를 줄였습니다.
- 기존 카메라, 4096 배경, 저장 시점 동작은 유지합니다.
