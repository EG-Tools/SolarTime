# Solar Time v0.16 업데이트

기준: GitHub `main` 커밋 `8386915ae85015ac2c51fbd86832e3edac0fb8ed` (v0.15 + 사용자가 복원한 4096×2048 우주 배경).

## 변경

- **행성 추적 중심 보정**: 가운데 버튼으로 화면을 옮긴 뒤 추적해도 기존 `panX/panY`를 목적지에 유지하지 않습니다. 추적 시 팬 오프셋이 0으로 부드럽게 수렴하여 천체가 화면 정중앙에 도착합니다.
- **추적 경로 블렌딩**: 서로 다른 추적 대상 사이에서 강제로 1× 개요 줌을 거치던 `bridge` 전환을 제거했습니다. 이전/다음 추적 천체의 월드 위치를 직접 보간하여 중간에 덜컥 꺾이는 움직임을 줄였습니다.
- **휠/핀치 확대 피벗 정리**: 일반 화면의 휠·핀치 확대는 더 이상 마우스 아래 천체나 태양을 숨은 `focus`로 만들지 않습니다. 명시적으로 `가까이 보기`, 더블클릭, 저장 시점을 사용할 때만 추적 대상이 생깁니다.
- **좌/우 자동 회전**: 팬 오프셋이 남은 경우 약 0.42초 동안 중앙으로 복귀한 뒤 자동 회전을 시작합니다. 오프센터 회전 때문에 확대/축소처럼 보이던 회전 반경 변화를 제거합니다.
- **한국 보기 확대**: `한국 보기`는 지구를 최소 224× 줌으로 이동합니다. 기존 최대 수동 확대 256×는 유지합니다.
- **배경 유지**: 우주 배경 이미지는 수정하지 않습니다. 현재 GitHub에 올린 4096×2048 원본을 그대로 사용합니다.

## 적용

ZIP 내용을 기존 SolarTime 프로젝트 루트에 같은 경로로 덮어쓴 뒤 GitHub에 올리면 됩니다. GitHub Pages에서는 별도 빌드가 필요 없습니다. 배포 후 브라우저에서 `Ctrl+F5`로 캐시를 새로 고치세요.

단일 HTML이 필요하면 Node.js 18 이상에서 `npm run build`를 실행하면 `dist/Solar-Time_v0.16.html`이 생성됩니다.

## 검사

- `node --check src/renderer.js src/app.js tools/build.cjs`: 통과
- `node --test tests/v016-camera.test.cjs`: 6/6 통과
- 수정된 카메라 회귀 검사 `tests/v015-camera.test.cjs`: 14/14 통과
- 수정된 빌드 검사 `tests/v015-build.test.cjs`: 3/3 통과 (변경 패키지 환경에서 누락된 기존 sky.js는 테스트용 placeholder로만 보완하여 검사)

Windows 실기기에서의 체감 FPS와 마우스 조작 감각은 배포 후 확인이 필요합니다.
