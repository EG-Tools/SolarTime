{
  "name": "eg-solar-time",
  "version": "0.0.34",
  "private": true,
  "description": "Offline solar-system clock with smooth camera transitions and a spherical night sky.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build": "node tools/build.cjs",
    "test:v034": "node --test tests/v034-release.test.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
