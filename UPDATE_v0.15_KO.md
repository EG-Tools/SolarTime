# Solar Time v0.15 업데이트

기준 저장소: EG-Tools/SolarTime
기준 커밋: d92e0e3ad2c78f58e0aee12a337ce3cfd747ec73
이번 ZIP은 이 커밋에 대한 변경·추가 파일만 포함합니다. 전체 프로젝트가 아닙니다.
GitHub에 직접 푸시하거나 배포 설정을 변경하지 않았습니다.

## GitHub 웹 버전에 적용

ZIP을 풀어 **내용물을 기존 SolarTime 프로젝트 루트에 같은 경로로 덮어쓴 뒤** 변경 파일을 GitHub에 올리세요. ZIP 파일 자체만 저장소에 올리거나 새 하위 폴더에 넣는 방식은 적용되지 않습니다.

이번 웹 배포에는 `npm run build`가 필요하지 않습니다. `index.html`이 새 `src/sky-asset.js`를 기존 이미지 묶음 뒤에 읽어 배경만 교체합니다. 기존 `src/assets.js`를 다시 만들지 않아도 새 배경이 표시됩니다. 다른 행성 이미지·별 자료는 기존 파일을 그대로 사용합니다.

Pages 배포 완료 후 기존 탭에서 Ctrl+F5로 새로고침하세요. 하단 v0.15 표기와 `SolarTime.version === '0.15'`로 버전을 확인할 수 있습니다. 새 스크립트 URL에는 `?v=0.15`를 붙였습니다.

## 변경 사항

### 카메라

천체 더블클릭·가까이 보기·한국/목성 특징 보기·기본 시점·저장 시점은 하나의 전환 처리기를 사용합니다. 현재 보이는 카메라에서 보간을 시작하며, 이동 중 새 명령이 들어오면 그 위치에서 이어집니다. 서로 다른 근접 추적 대상은 전체 구도를 거쳐 전환하고, 기본 시점으로 나갈 때는 줌이 1이 될 때까지 기존 추적 기준을 유지합니다.

휠·확대 버튼·방향키·각도 조절·드래그·핀치에는 짧은 보간을 적용했습니다. 연속 휠은 현재 화면값이 아니라 누적 목표 배율을 사용해 입력이 유실되지 않습니다. 드래그는 시작 좌표와 전체 이동량으로 목표를 계산합니다. 카메라 이동 때문에 공전·자전 시간은 바뀌지 않습니다.

초기 설정 복구나 외부 코드용 즉시 설정 API는 유지했습니다. 사용자가 조작하는 위 항목들은 보간 API를 사용합니다. 카메라 화면 크기 한도·시점 저장 키·스키마는 유지했습니다.

### 단축키 1·2·3

앱에 키보드 포커스가 있는 동안, 버튼·감상 모드·전체 화면·열린 대화상자·입력란에서도 작동합니다. 숫자패드도 지원합니다. 숨겨진 달·명왕성의 저장 시점은 해당 천체 표시를 다시 켭니다. 빈 슬롯은 비어 있다는 안내만 표시하며 임의의 시점을 만들지 않습니다.

날짜 입력을 망가뜨리지 않도록 입력란에서는 시점 호출과 함께 원래 숫자 입력도 허용하고 날짜 대화상자를 닫지 않습니다. Ctrl·Alt·Meta 조합은 브라우저/운영체제에 남깁니다. IME 조합 중의 키는 가로채지 않습니다. 앱 바깥이나 브라우저 주소창의 입력은 웹 앱이 받을 수 없습니다.

### F / ESC

F와 전체 화면 버튼은 동일한 토글을 사용합니다. **전체 화면에서 F와 ESC는 동일한 종료 함수**를 호출합니다. ESC는 전체 화면에 들어가는 토글을 호출하지 않습니다.

| 현재 상태 | 짧게 ESC 한 번 |
|---|---|
| 감상 모드 | 감상 모드 종료 |
| 전체 화면 | 전체 화면 종료 |
| 전체 화면 + 감상 모드 | 감상 모드만 종료, 전체 화면 유지 |
| 위 상태에서 한 번 더 ESC | 전체 화면 종료 |
| 두 모드 모두 아님 | 열린 대화상자/패널 정리 |

키를 계속 누르는 반복 keydown으로 두 단계가 한 번에 처리되지 않도록 했습니다.

브라우저가 ESC를 먼저 가져가지 않도록 전체 화면에 진입하면 `navigator.keyboard.lock(['Escape'])`를 요청하고, 종료하면 해제합니다. **지원 및 권한 허용이 있어야 두 단계 순서를 보장할 수 있습니다.** 미지원·권한 거부에서는 브라우저가 먼저 전체 화면을 종료할 수 있습니다. 이때 F로 종료하는 기능은 유지하며, 강제로 전체 화면을 재진입시키지 않습니다. ESC 길게 누르기 등 브라우저의 강제 탈출 기능은 막지 않습니다. F11 전체 화면과 운영체제 창 최대화는 앱의 F 전체 화면과 별개입니다.

참고: Chrome Developers, Better full screen mode with the Keyboard Lock API
https://developer.chrome.com/blog/better-full-screen-mode

### 배경

사용자가 선택한 어두운 배경과 기존 별·은하 구도를 유지하고, 이전에 보정한 가스 띠의 연결부를 반영했습니다. 검사 미리보기의 밝기 증폭을 제거한 뒤 보정 차이만 원본에 옮겨 작은 별과 은하 중심이 불필요하게 뭉개지지 않도록 했습니다.

출력은 **4096×2048 무손실 WebP**입니다. 원본 첨부는 2048×1024, 보정 미리보기는 1536×768이므로 출력은 **확대·보정본이며 네이티브 4K 새 사진이 아닙니다.** 크기 변경 후 좌우 경계를 다시 보정하고 북극·남극 행의 색을 균일하게 만들었습니다. 배포 WebP를 재디코딩해 좌우 경계 일치·극점 균일성을 확인했습니다. 런타임에서 매 프레임 안개를 생성하지 않습니다.

`assets/universe.webp`와 `src/sky-asset.js`는 동일한 이미지입니다. 이후 이미지를 다시 교체할 때는 `node tools/pack_assets.cjs` 또는 `npm run build`로 두 표현을 함께 갱신하세요. 변경하지 않은 행성 이미지는 이번 ZIP에 중복 포함하지 않았습니다.

## 단일 실행 HTML이 필요한 경우

기존 전체 프로젝트에 이 패치를 적용하고 아래 명령을 실행합니다.

```text
npm run build
```

결과: `dist/Solar-Time_v0.15.html`

이 명령은 기존 행성 이미지를 포함해 `src/assets.js`와 배경 전용 묶음을 재생성하고, 배경을 한 번만 포함한 단일 HTML을 만듭니다. 기존 dist의 이전 버전은 덮어쓰지 않습니다. 이번 ZIP에는 완성 단일 HTML은 포함하지 않았습니다. GitHub Pages 웹 버전 적용에는 이 명령이 필요하지 않습니다.

‘이미지 포함 HTML 저장’의 저장 파일명도 v0.15로 수정했습니다. 내보내기에는 현재 배경을 포함하고 별도 배경 묶음이 중복 들어가지 않도록 했습니다.

## 검증 범위

- Node 단위·빌드 검사 26개 통과: 기존 v0.14 계산/캐시 검사 11개 + 신규 카메라·배경 묶음 검사 12개 + 빌드 직렬화 검사 3개.
- Chromium 동작 검사 24개 통과: 실제 app.js / renderer.js / materials.js / sky.js를 사용하고, 천문·표면·저장소·CSS는 테스트용 대체 구성으로 분리했습니다.
- 두 단계 ESC의 권한 허용/거부 케이스는 브라우저 Fullscreen/Keyboard API를 모의한 제어 흐름 검사입니다. 별도로 실제 Chromium Fullscreen API의 F 진입/종료를 확인했습니다. 실제 OS 키 가로채기·권한 프롬프트까지 검증한 것은 아닙니다.
- WebP 디코딩 후 픽셀 보존, 좌우 동일 경계, 상하 극점 균일 검사 통과.

전체 기존 앱 회귀 검사·Windows 실기기 검증·실기기 FPS 측정은 수행하지 않았습니다. 이 환경에서는 공개 웹 및 로컬 HTTP 페이지 접근이 제한되어, 브라우저 검사는 오프라인 문서에 실제 변경 코드를 넣어 진행했습니다.

검사 결과는 `docs/unit-verification-v0.15.txt`, `docs/ui-verification-v0.15.json`, `docs/sky-verification-v0.15.json`, 파일 목록·해시는 `docs/manifest-v0.15.json`에 있습니다.

신규 Node 검사 재실행: `npm run test:v015`
브라우저 검사 재실행: `python tests/v015_ui_browser.py` (Playwright와 Chromium 필요; `CHROMIUM` 환경변수로 실행 파일 지정 가능)
