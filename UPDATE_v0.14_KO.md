# Solar Time v0.14 변경 파일

대상: EG-Tools/SolarTime, main 커밋 `67234aa88a971fb7a0ec87434f9d4e37dcfc752c`의 v0.13.
이 ZIP은 전체 프로젝트가 아닙니다. 변경·추가한 파일만 들어 있으며, 기존 행성 이미지나 v0.13 실행 HTML을 중복해서 넣지 않았습니다. GitHub 푸시·배포는 하지 않았습니다.

## 실행 HTML만 사용하는 경우 — 설치 없이 적용

1. ZIP 안의 `SolarTime_Update_v0.14.html`을 Chrome 또는 Edge에서 엽니다.
2. 기존 `dist/Solar-Time_v0.13.html`을 선택합니다. 사진을 추가 저장한 v0.13 HTML도 지원합니다.
3. `v0.14 실행 파일 만들기` → `v0.14 다운로드`를 누르고, 새 `Solar-Time_v0.14.html`을 엽니다.

업데이트 도구는 원본 파일을 수정하지 않고 새 파일을 다운로드합니다. 선택한 HTML의 스크립트를 실행하지 않으며, 파일 업로드나 네트워크 요청도 하지 않습니다. 기존 행성 이미지·사진 출처·설정 및 시점 저장 키·스타일·시간 모델은 유지하고 배경과 렌더러를 교체합니다. 개발용 `index.html`, v0.12 이하 또는 v0.14 이상은 입력으로 받지 않습니다.

**도구 HTML 자체는 태양계 시계가 아닙니다. 변환한 `Solar-Time_v0.14.html`이 실행 파일입니다.**

## GitHub 프로젝트 소스를 사용하는 경우

ZIP의 내용을 기존 v0.13 SolarTime 프로젝트 루트에 같은 경로로 덮어쓴 뒤 아래를 실행합니다. Node.js 18 이상이 필요하며, `npm install`은 필요 없습니다.

```text
npm run build
```

생성되는 파일: `dist/Solar-Time_v0.14.html`.

빌드는 기존 행성 WebP와 새 `assets/universe.webp`를 합쳐 `src/assets.js`를 다시 생성합니다. `index.html`의 버전 표기와 `src/materials.js`의 사진 저장 파일명도 v0.14로 갱신합니다. 이전 `dist/Solar-Time_v0.13.html`은 덮어쓰지 않습니다.

**소스만 덮어쓰고 빌드를 생략하면, 기존 src/assets.js 또는 이전 dist HTML에는 옛 배경이 남아 있습니다.** 개발용 index.html도 빌드 후 사용하세요. 브라우저에 이전 소스가 캐시되어 있으면 Ctrl+F5로 새로 고침합니다.

두 적용 방법 중 하나만 선택하면 됩니다. HTML 변환은 실행 파일만 갱신하며 GitHub 프로젝트 소스는 바꾸지 않습니다.

## 무엇을 바꿨는가

### 구면 전체를 위한 우주 배경

4096×2048, 2:1 등거리 원통형(equirectangular) WebP를 새로 제작했습니다. 무손실 저장이며 약 1.11 MB입니다. 저장소의 기존 배경 2,048,536바이트에서 1,113,268바이트로 약 45.7% 줄었습니다. 이 수치는 파일 크기이며 GPU 메모리나 FPS 개선율을 뜻하지 않습니다.

성운과 먼지층은 3차원 방향 좌표의 연속적인 주기 노이즈로 생성했습니다. 은하는 구의 접평면에서 원형으로 감쇠시키고, 사각형 사진 조각은 사용하지 않습니다. 좌우 경도를 가로지르는 별도 잘리지 않게 감쌌습니다. 극점은 같은 방향에 여러 색이 모이지 않도록 단색으로 수렴시켰습니다. GPU와 CPU 모두 경도 방향을 감싸며 보간합니다.

좌우 끝 픽셀은 구면에서 서로 이웃한 서로 다른 위치이므로 반드시 같은 색일 필요는 없습니다. 두 끝을 보간해서 이어지는 것이 정상입니다. 반면 맨 위와 아래의 극점 행은 각각 하나의 색으로 통일했습니다. 지구에서 관측한 실제 별자리·실시간 천문 사진이 아닌 감상용 우주입니다.

### 렌더링 부하 줄이기

카메라 방향의 삼각함수, 천체 표면 좌표계, 궤도 투영 좌표, 앞뒤 궤도 Path2D, 이름 너비, 별빛 이미지를 캐시합니다. 추적 중 화면 중심만 움직일 때 궤도는 저장한 경로를 평행 이동하므로 같은 점을 반복 투영하지 않습니다. 확대·시점 회전·모델 연도 변경은 필요한 캐시를 갱신합니다. 지구·달의 조명 계산에 사용하는 지구 위치도 같은 시각·모델에서 공유합니다.

배경만 저속으로 흐를 때는 GPU 배경 갱신을 최대 약 30회/초로 제한합니다. 사용자가 카메라를 돌리거나 카메라 자동 회전을 할 때는 이 제한을 적용하지 않습니다. 행성·시계·태양 샤인·별 반짝임의 갱신 속도를 30으로 내리는 변경이 아닙니다. 배경과 카메라가 정지하면 불필요한 배경 드로우를 생략합니다.

GPU를 사용할 수 없는 경우, CPU 배경의 광선 계산표와 감마 변환표를 재사용합니다. 세로로 긴 화면에서도 계산 픽셀 수가 과도해지지 않도록 이동 중 90,000, 정지 시 160,000픽셀 이내로 제한합니다. 이는 호환용 배경에만 적용되며 행성 표면의 기존 고해상도 규칙은 유지합니다.

### 안정성

탭을 숨길 때 대기 중인 CPU 배경 작업을 취소하고 완료된 이미지·디코딩 재질은 유지합니다. 복귀 시 숨겨진 시간만큼 배경이 갑자기 회전하지 않습니다. WebGL 컨텍스트 복구 시 정지된 배경도 다시 그립니다. WebGL을 잡은 뒤 셰이더가 실패한 경우에는 같은 캔버스에 2D 컨텍스트를 요청하는 대신, 배경 캔버스만 교체하여 CPU 출력으로 전환합니다.

기존 카메라 1·2·3 저장 스키마와 저장 키, 최대 256배 확대, 64배 이하 기존 시점 크기, 가운데 버튼 이동 범위, 공전·자전 계산 및 시간 배율은 바꾸지 않았습니다. 저장 시점이 새 HTML에서 공유되는지는 기존과 마찬가지로 브라우저의 파일별 저장소 정책에 따릅니다.

## 검사 결과와 범위

- 새 단위 테스트 15개 통과: 좌표·궤도 경로·캐시·확대·구면 보간·업데이트 변환·빌드 실패 처리. 천문 계산이 아닌 명시적인 테스트용 천체를 사용합니다.
- Chromium 배경 검사 20개 통과: 실제 새 배경의 WebGL 출력, 고정 화면 재사용, 수동 조작, 컨텍스트 손실/복원, CPU 대체 출력, 셰이더 오류 후 전환, 세로 화면, 일시 중지/재개.
- 업데이트 도구 검사 8개 통과: 파일 선택, HTML 다운로드, 이미지·저장 키 보존, 원본 불변, 입력 스크립트 미실행, 네트워크 요청 없음, 잘못된 파일 거절.

WebGL 검사는 Linux Chromium + SwiftShader에서 수행했습니다. 업데이트 UI는 관리형 테스트 브라우저가 file: 탐색을 막아 동일 HTML을 메모리에 로드하여 검사했습니다. 기존 v0.13 완성 앱 전체와 그 앱의 기존 회귀 테스트 전체를 실행한 것은 아니며, 사용자 Windows GPU의 실제 FPS와 다양한 드라이버 호환성은 측정하지 않았습니다. 테스트용 HTML은 원본 완성 앱이 아닙니다. 미측정 FPS 개선율을 보장하지 않습니다.

새 테스트만 실행:

```text
node --test tests/v014-renderer.test.cjs tests/v014-upgrade.test.cjs
python tests/v014_sky_browser.py
python tests/v014_upgrade_browser.py
```

Python 브라우저 검사에는 Playwright/Chromium이 필요합니다. Linux에서 디스플레이가 없으면 `xvfb-run -a python tests/v014_sky_browser.py`로 실행합니다. Chromium 위치는 `CHROMIUM` 환경변수로 지정할 수 있습니다. 기존 프로젝트의 `npm test`는 기존 테스트도 함께 실행합니다.

배경을 다시 제작하려는 개발자만 `python tools/bake_sky.py`를 사용합니다. 이 제작 단계에는 NumPy, SciPy, Pillow가 필요하며, 시계 실행에는 필요하지 않습니다. 업데이트 도구 HTML을 다시 생성할 때는 `npm run build:update`를 사용합니다.

## 파일 구성

수정: `src/renderer.js`, `src/sky.js`, `assets/universe.webp`, `tools/bake_sky.py`, `tools/build.cjs`, `package.json`.
추가: 설치 없는 업데이트 도구 HTML, `tools/upgrade-core.js`, `tools/build-update.cjs`, 신규 검사 코드, 검사 결과 및 이 안내문.

기존 행성 재질과 출처·이용 조건은 유지합니다. 새 우주 배경은 이 버전의 제작 코드로 생성한 절차적 이미지로, 기존 assets/sky의 사진 조각을 사용하지 않습니다.
